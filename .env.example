# =============================================================================
# InviteChat.site Environment Variables
# =============================================================================
# Copy this file to .env and fill in your values
# =============================================================================

# -----------------------------------------------------------------------------
# Server Configuration
# -----------------------------------------------------------------------------
PORT=3000
# Set to true in production, false for development
PRODUCTION=false

# -----------------------------------------------------------------------------
# Stripe Configuration
# https://dashboard.stripe.com/apikeys
# -----------------------------------------------------------------------------
# Publishable key (starts with pk_test_ or pk_live_)
# Found at: Stripe Dashboard > Developers > API keys > Publishable key
STRIPE_PUBLISHABLE_KEY=pk_test_xxxxxxxxxxxxxxxxxxxxx

# Secret key (starts with sk_test_ or sk_live_)
# Found at: Stripe Dashboard > Developers > API keys > Secret key
# WARNING: Never commit this to version control!
STRIPE_SECRET_KEY=sk_test_xxxxxxxxxxxxxxxxxxxxx

# -----------------------------------------------------------------------------
# Discord Configuration
# https://discord.com/developers/applications
# -----------------------------------------------------------------------------
# Application ID / Client ID
# Found at: Discord Developer Portal > Your App > General Information > Application ID
# Also visible at: OAuth2 > Client ID
DISCORD_CLIENT_ID=

# Client Secret
# Found at: Discord Developer Portal > Your App > OAuth2 > Client Secret
# Click "Reset Secret" if you don't see it
# WARNING: Never commit this to version control!
DISCORD_CLIENT_SECRET=

# Bot Token
# Found at: Discord Developer Portal > Your App > Bot > Token
# Click "Reset Token" if you don't see it
# Make sure to enable: Privileged Gateway Intents > Server Members Intent
# WARNING: Never commit this to version control!
DISCORD_BOT_TOKEN=

# OAuth2 Redirect URI
# Must match EXACTLY what you set in:
# Discord Developer Portal > Your App > OAuth2 > Redirects
# Format: https://yourdomain.com/redirect
DISCORD_REDIRECT_URI=https://yourdomain.com/redirect

# Guild (Server) ID
# How to get: Enable Developer Mode in Discord (User Settings > App Settings > Advanced)
# Then right-click your server name > Copy Server ID
DISCORD_GUILD_ID=

# Role ID to assign to premium members
# How to get: Server Settings > Roles > Right-click the role > Copy Role ID
# Make sure the bot's role is HIGHER than this role in the role hierarchy
DISCORD_ROLE_ID=

# Discord Server Invite Link (auto-generated, never expires)
# This is created by the bot with maxAge=0 and maxUses=0
DISCORD_INVITE=

# -----------------------------------------------------------------------------
# Telegram Configuration
# https://core.telegram.org/bots#botfather
# -----------------------------------------------------------------------------
# Bot Token from @BotFather
# Create a bot: Message @BotFather > /newbot > follow prompts
# Token looks like: 123456789:ABCdefGHIjklMNOpqrsTUVwxyz
# WARNING: Never commit this to version control!
TELEGRAM_BOT_TOKEN=

# Bot Username (without @ symbol)
# Example: If your bot is @MyPremiumBot, enter: MyPremiumBot
TELEGRAM_BOT_USERNAME=

# Private Group ID (negative number)
# How to get: Add @userinfobot to your group temporarily
# It will post the group ID (e.g., -1001234567890)
# Remove @userinfobot after getting the ID
TELEGRAM_GROUP_ID=

# -----------------------------------------------------------------------------
# Session Configuration
# -----------------------------------------------------------------------------
# Random secret string for signing session cookies
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=your_random_secret_here_at_least_32_characters
